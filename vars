export PERL_CARTON_PATH=$(pwd)/local
export PERL5LIB=$(pwd)/lib/perl5:$PERL_CARTON_PATH/lib/perl5:$PERL5LIB
export PATH=$PERL_CARTON_PATH/bin:$(pwd)/bin:$PATH

export SLOTS_CONF=$(pwd)/../configs/slots.ini
export CSG_MAPPING_CONF=$(pwd)/../configs/mapper.ini
